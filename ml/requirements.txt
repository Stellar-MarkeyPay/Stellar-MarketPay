# ML ranking training dependencies (Issue #265 — pinned for reproducibility)
# Pin exact versions so every training run uses identical packages.
# Regenerate with: pip-compile ml/requirements.in -o ml/requirements.txt
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
lightgbm==4.3.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pyyaml==6.0.1
